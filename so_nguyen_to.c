#include <stdio.h>
int main () {
	int a;
	printf ("Nhap vao so: ");
	scanf ("%d", &a);
	if (a<3) {
		swtich (a%2) {
			case 0 :
				printf ("Day la so nguyen to")
			case 1 :
				printf ("Day khong la so nguyen to")
	if a>2 
